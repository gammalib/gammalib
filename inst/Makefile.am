## Process this file with automake to produce Makefile.in

# Detect configuration
if WITH_INST_CTA
  INST_CTA = cta
endif
if WITH_INST_LAT
  INST_LAT = lat
endif

# Set sub directories
SUBDIRS = $(INST_CTA) $(INST_LAT)

